#!/usr/bin/python3
def divisible_by_2(my_list=[]):
    my_even = []
    for i in range(len(my_list)):
        if my_list[i] % 2 == 0:
            my_even.append(True)
        else:
            my_even.append(False)
    return (even)
